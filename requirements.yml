---
# Collections Ansible requises — Groupe 3 SOC
# Installation : ansible-galaxy collection install -r requirements.yml

collections:
  # pfSense automation (playbook 08)
  - name: pfsensible.core
    version: ">=0.6.0"

  # MySQL/MariaDB modules (playbook 05 GLPI)
  - name: community.mysql
    version: ">=3.0.0"

  # Windows modules (domain join, WinRM)
  - name: ansible.windows
    version: ">=2.0.0"

  # General utils
  - name: community.general
    version: ">=7.0.0"
